# Web framework for building the application interface and REST API
Flask==2.0.1

# HTTP library for making API requests to rqlite nodes
requests==2.26.0

# In-memory data store for caching and session management
redis==4.1.0

# Monitoring and metrics collection library
prometheus-client==0.12.0

# Library for handling dates and times
python-dateutil==2.8.2

# Extension for handling Cross-Origin Resource Sharing (CORS)
flask-cors==3.0.10 